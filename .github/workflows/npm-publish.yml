name: Publish to npm on push (latest)

on:
  push:
    branches: [main]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Install deps
        run: npm ci

      - name: Test (optional)
        run: npm test --if-present

      - name: Build (optional)
        run: npm run build --if-present

      - name: Bump unique version (no commit)
        # Делает уникальную версию на каждый запуск (пример: 0.1.1-ci.20260126.1215)
        run: npm version prerelease --preid=ci.$(date -u +%Y%m%d.%H%M) --no-git-tag-version

      - name: Publish to npm (latest)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public